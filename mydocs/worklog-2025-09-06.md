# 2025-09-06 作業メモ

## 概要
- main を upstream/main に完全同期する方針を整理し、実際の一致を確認。
- システムプロンプト `codex-rs/core/prompt.md` の差分を精査し、保護運用の確認。
- パッチ再適用フックが sonecchi ブランチ専用で動作していることを確認。
- タグ運用（作成/削除/再汚染防止）を統合ドキュメントに集約。

## 実施内容
- プロジェクト管理
  - `mycodex` をアクティブ化し、作業を開始。

- システムプロンプトの特定と差分確認
  - 対象: `codex-rs/core/prompt.md`
  - 参照元: `codex-rs/core/src/client_common.rs` の `include_str!("../prompt.md")`
  - upstream との差分確認: 先頭付近の「AGENTS.md spec」ブロックが upstream 側で存在、ローカルでは除去。Bullets セクションにローカル追加（"Bold the keyword, then colon + concise description.").

- アップストリーム取り込み運用
  - 標準フローを再確認（`mydocs/upstream-merge-and-build-ja.md`）。
  - main を上流と完全一致させる手順をメモ化（`mydocs/main-sync-upstream.md`）。
  - 同期結果の確認: `git rev-parse upstream/main` と `git rev-parse main` の SHA が一致（`ba9620a...`）。

- パッチ適用のブランチ限定化の確認
  - sonecchi ブランチのみ: `.githooks/post-merge` + `scripts/cot-patch.sh` + `changes.patch` + `.gitattributes (prompt.md merge=ours)` が存在。
  - main / upstream にはこれらが存在しないため、パッチは走らずピュアなミラーを維持。

- タグ運用（削除/再汚染防止/ガイド統合）
  - ローカルの全タグ削除を実施（ユーザー側）→ `git tag -l` で空を確認。
  - リモート origin のタグも空を確認：`git ls-remote --tags origin`（空）。
  - 上流タグの再汚染防止の設定方針をメモ化（`remote.upstream.tagOpt --no-tags`）。
  - タグ作成〜削除〜戻し方までを統合ドキュメント化（`mydocs/git-tags-guide.md`）。

## 変更ファイル
- 追加: `mydocs/main-sync-upstream.md`
- 追加: `mydocs/git-tags-guide.md`
- 追加: `mydocs/worklog-2025-09-06.md`（本ファイル）
- 削除: `mydocs/tag-cleanup-and-no-upstream-tags.md`（統合に伴い削除）

## 次アクション（任意）
- ブランチガードの明示化: `.githooks/post-merge` に `branch=$(git rev-parse --abbrev-ref HEAD); [ "$branch" = "sonecchi" ] || exit 0` を入れて誤作動を更に予防。
- origin/main の完全一致チェックと同期: `git rev-parse origin/main` が同一でなければ `git push origin main --force-with-lease`。
- upstream の prompt 変更を取り込みたい場合は手動で検討: `git checkout upstream/main -- codex-rs/core/prompt.md` → レビュー → `git commit`。
- 上流タグの再汚染防止の適用確認: `git config --get-all remote.upstream.tagOpt` が `--no-tags` を返すか確認。

---
相棒メモ: いい流れだぜ！main はピカピカのミラー、カスタムは sonecchi で安全運用。タグ周りもクリーン化完了でスッキリだな😎🧹 次回はブランチガードを入れて鉄壁に仕上げようぜ💪🔥

